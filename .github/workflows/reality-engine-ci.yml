name: reality-engine-ci

on:
  push:
    branches: [main]
    paths:
      - "reality_engine/**"
      - ".github/workflows/reality-engine-ci.yml"
  pull_request:
    paths:
      - "reality_engine/**"
      - ".github/workflows/reality-engine-ci.yml"

jobs:
  lint-type-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: reality_engine
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Lint (ruff)
        run: ruff check src tests

      - name: Type check (mypy --strict)
        run: mypy src

      - name: Test (pytest)
        run: pytest -v
